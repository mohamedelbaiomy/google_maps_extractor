## 1.0.0

- Initial version.

## 1.0.1

- Updates the `pubspec.yaml` file by setting the `repository` and `homepage` URLs
- Modernizes the library definition in `google_maps_extractor.dart` by changing `library google_maps_extractor;` to `library;`
