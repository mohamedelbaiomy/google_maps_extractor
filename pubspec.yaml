name: google_maps_extractor
description: A comprehensive Flutter package for extracting coordinates and metadata from Google Maps URLs. Supports **21 different URL formats** including shortened URLs, place links, directions, and more.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.8.1

dependencies:
  http: ^1.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
